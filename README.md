# University Management System (Java Swing + SQL)

## 📌 Overview
A desktop-based University Management System developed using **Java Swing** for the user interface and **MySQL** for database management. The project demonstrates CRUD operations (Create, Read, Update, Delete) with secure JDBC connectivity using the MySQL Connector JAR.

## ⚙️ Tech Stack
- Java Swing (UI)
- MySQL Database
- JDBC (SQL Connector JAR)
- NetBeans IDE

## 🚀 Features
- Student registration and record management
- Teacher details and leave management
- Course creation and enrollment
- Examination and marks entry
- Fee structure and student fee forms
- CRUD operations for student and course data
- User-friendly forms and tables

## 📂 Project Structure
- `src/university/management/system/` → Java source files (e.g., `AddStudent.java`, `Login.java`, `FeeStructure.java`)
- `lib/` → MySQL Connector JAR
- `nbproject/` → NetBeans project configuration

## 🔧 Setup Instructions
1. Clone the repository:
   ```bash
   git clone https://github.com/Kavyashree34/university-management-system-java-swing.git
